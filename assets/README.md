# Assets

Place optional binary assets (for example `icon.ico`) in this folder before
running the packaging scripts. The repository intentionally ignores binary
artifacts, so copy or generate them locally as needed.
